{
  "name": "leaflet-svg-viewer",
  "version": "0.0.1",
  "description": "Leaflet SVG viewer for non-cartographic high-detailed schematics",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "watchify -v -d -t [ babelify --presets [ es2015 ] ] demo/js/index.js -o demo/js/build.js & serve -p 3002",
    "build": "browserify -v -d -t [ babelify --presets [ es2015 ] ] demo/js/index.js -o demo/js/build.js"
  },
  "author": "Alexander Milevski <info@w8r.name>",
  "license": "MIT",
  "dependencies": {
    "leaflet": "^0.7.7",
    "xhr": "^2.2.0"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.3.13",
    "babelify": "^7.2.0",
    "browserify": "^13.0.0",
    "faucet": "0.0.1",
    "tape": "^4.4.0",
    "watchify": "^3.7.0"
  }
}
